import {
  Text,
  Image,
  TextInput,
  KeyboardAvoidingView,
  ImageBackground,
  TouchableOpacity,
  View,
  ScrollView,
} from 'react-native';
import React, {useState} from 'react';

const Chat = ({navigation}) => {
  return (
    <View>
      <Text>Chat Page</Text>
    </View>
  );
};

export default Chat;
