import {View, Text} from 'react-native';
import React from 'react';

const index = () => {
  return (
    <View
      style={{
        backgroundcolor: '#000',
      }}>
      <Text>index</Text>
    </View>
  );
};

export default index;
