import {View, Text} from 'react-native';
import React from 'react';

const index = () => {
  return (
    <View
      style={{
        backgroundColor: 'black',
      }}>
      <Text>index</Text>
    </View>
  );
};

export default index;
